# Favorites visible surface — source-of-truth cleanup

## Scope

- Replace placeholder values in favorites.title

- Replace placeholder values in favorites.empty.noFav

- Keep Java untouched in this branch

- Keep debug/internal/SQL literals out of scope

## Why this is strong

- visible-surface unit

- live-path only

- low blast radius

- safe rollout with gates + runtime

